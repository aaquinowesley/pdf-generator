<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        table{
            width: 100%;
        }
        td{
            border: 1px solid rgb(0, 0, 0);
        }
        tr{
            text-align: left;
            border: 1px solid rgb(0, 0, 0);
        }
        th{
            border: 1px solid rgb(0, 0, 0);
        }
    </style>
</head>
<body class="bg-purple-100">
    <h1 class="text-3xl text-center">Restaurante</h1>
    <div class="m-8">
    <table>
        <thead>
            <tr>
                <th>Nome:</th>
                <th>Pedido:</th>
                <th>Hora do Pedido:</th>
                <th>Valor:</th>
            </tr>
        </thead>
        <tbody>
            <% if (pedidos.length > 0) { %>
                <% pedidos.forEach(pedido => { %>
                    <tr>
                        <td><%= pedido.nome %></td>
                        <td><%= pedido.pediu %></td>
                        <td><%= pedido.horario %></td>
                        <td>R$ <%= pedido.valor  %> </td>
                    </tr>
                <%  }); %>
            <% } %>
        </tbody>
        <tfoot>
            <tr><td><%=pedidos.length%> Total de Pedidos</td></tr>
        </tfoot>
    </table>
    </div>
    <script src="https://cdn.tailwindcss.com"></script>
</body>
</html>